@using SUVAN.BackOffice.Models.ViewModel.Enums;
@using SUVAN.BackOffice.Portal.Helper;
@inject IUsuarioHelper UsuarioHelper

@{
    ViewData["Title"] = "Modelos de llantas";
}
@model List<SUVAN.BackOffice.Models.ViewModel.Administrativo.LlantaModeloViewModel>

@{
    var permisosPagina = await UsuarioHelper.GetPermisosPagina(User, EnumOpcionMenu.LlantasModelo);
    if (permisosPagina == null)
    {
        return;
    }
}

<!--begin::Toolbar-->
<div id="kt_app_toolbar" class="app-toolbar ">
    <div id="kt_app_toolbar_container" class="app-container container-fluid d-flex align-items-stretch">
        <!--begin::Toolbar wrapper-->
        <div class="app-toolbar-wrapper d-flex flex-stack flex-wrap gap-4 w-100">
            <!--begin::Page title-->
            <div class="page-title d-flex flex-column justify-content-center gap-1 me-3">
                <!--begin::Title-->
                <h1 class="page-heading d-flex flex-column justify-content-center text-dark fw-bold fs-3 m-0">Modelos de llantas</h1>
                <!--end::Title-->
            </div>
            <!--end::Page title-->
            <!--begin::Actions-->
            <div class="d-flex align-items-center gap-2 gap-lg-3">
                @if (permisosPagina.Agregar)
                {
                    <a asp-controller="LlantasModelo" asp-action="Crear" class="btn btn-flex btn-primary h-40px fs-7 fw-bold">
                        Nuevo modelo
                    </a>
                }
            </div>
            <!--end::Actions-->
        </div>
        <!--end::Toolbar wrapper-->
    </div>
    <!--end::Toolbar container-->
</div>
<!--end::Toolbar-->

<!--begin::Content-->
<div id="kt_app_content" class="app-content flex-column-fluid">
    <input type="hidden" id="mensajeTempData" value="@TempData["Mensaje"]" />
    <!--begin::Content container-->
    <div id="kt_app_content_container" class="app-container container-fluid">
        <!--begin::Card-->
        <div class="card">
            <!--begin::Card header-->
            <div class="card-header border-0 pt-6">
                <!--begin::Card title-->
                <div class="card-title">
                    <!--begin::Search-->
                    <div class="d-flex align-items-center position-relative my-1">
                        <i class="ki-outline ki-magnifier fs-3 position-absolute ms-5"></i>
                        <input type="text" data-kt-llantasmodelo-table-filter="search" class="form-control form-control-solid w-450px ps-13" placeholder="Buscar" />
                    </div>
                    <!--end::Search-->
                </div>
                <!--begin::Card title-->
            </div>
            <!--end::Card header-->

            <!--begin::Card body-->
            <div class="card-body py-4">
                <!--begin::Table-->
                <table class="table align-middle table-row-dashed fs-6 gy-5" id="kt_table_llantasmodelo">
                    <thead>
                        <tr class="text-start text-muted fw-bold fs-7 text-uppercase gs-0">
                            <th class="min-w-80px">Id</th>
                            <th class="min-w-160px">Marca</th>
                            <th class="min-w-180px">Modelo</th>
                            <th class="min-w-125px">Medida</th>
                            <th class="min-w-125px">Presión PSI</th>
                            <th class="min-w-150px">Profundidad mm</th>
                            <th class="min-w-150px">Vida útil km</th>
                            <th class="min-w-125px">Estatus</th>
                            <th class="min-w-150px">Fecha creación</th>
                            @if (permisosPagina.Editar || permisosPagina.Eliminar)
                            {
                                <th class="text-end min-w-100px">Acciones</th>
                            }
                        </tr>
                    </thead>
                    <tbody class="text-gray-600 fw-semibold">
                        @foreach (var modelo in Model)
                        {
                            <tr>
                                <td>@modelo.IdModeloLlanta</td>
                                <td>@modelo.Marca</td>
                                <td>@modelo.Nombre</td>
                                <td>@(modelo.Medida ?? "-")</td>
                                <td>
                                    @if (modelo.PresionMinimaPsi.HasValue || modelo.PresionMaximaPsi.HasValue)
                                    {
                                        <span>@(modelo.PresionMinimaPsi?.ToString("0.##") ?? "-") / @(modelo.PresionMaximaPsi?.ToString("0.##") ?? "-")</span>
                                    }
                                    else
                                    {
                                        <span>-</span>
                                    }
                                </td>
                                <td>@(modelo.ProfundidadOriginalMm?.ToString("0.##") ?? "-")</td>
                                <td>@(modelo.VidaUtilEstimadaKm?.ToString("N0") ?? "-")</td>
                                <td>
                                    @if (modelo.Activo)
                                    {
                                        <span class="badge badge-light-success">Activo</span>
                                    }
                                    else
                                    {
                                        <span class="badge badge-light-danger">Inactivo</span>
                                    }
                                </td>
                                <td>@modelo.FechaCreacion.ToString("dd/MM/yyyy")</td>
                                @if (permisosPagina.Editar || permisosPagina.Eliminar)
                                {
                                    <td class="text-end">
                                        @if (permisosPagina.Editar)
                                        {
                                            <a asp-controller="LlantasModelo" asp-action="Editar" asp-route-id="@modelo.IdModeloLlanta" class="btn btn-icon btn-light btn-active-light-primary btn-sm" title="Editar">
                                                <i class="ki-outline ki-pencil fs-3"></i>
                                            </a>
                                        }
                                        @if (permisosPagina.Eliminar)
                                        {
                                            <button type="button"
                                                    class="btn btn-icon btn-light btn-active-light-danger btn-sm"
                                                    data-kt-llantasmodelo-table-filter="delete_row"
                                                    data-kt-llantamodelo-delete-item="@modelo.IdModeloLlanta"
                                                    data-kt-llantamodelo-delete-name="@modelo.Nombre"
                                                    title="Eliminar">
                                                <i class="ki-outline ki-trash fs-3"></i>
                                            </button>
                                        }
                                    </td>
                                }
                            </tr>
                        }
                    </tbody>
                </table>
                <!--end::Table-->
            </div>
            <!--end::Card body-->
        </div>
        <!--end::Card-->
    </div>
    <!--end::Content container-->
</div>
<!--end::Content-->

@section Scripts {
    @Html.AntiForgeryToken()
    <script src="~/js/administrativo/llantasmodelo/table.js" asp-append-version="true"></script>
    <script src="~/assets/js/widgets.bundle.js"></script>
    <script src="~/assets/js/custom/widgets.js"></script>
}
