@using SUVAN.BackOffice.Models.ViewModel.Comercial;
@using SUVAN.BackOffice.Models.ViewModel.Enums;
@using SUVAN.BackOffice.Portal.Helper;
@inject IUsuarioHelper UsuarioHelper
@{
    ViewData["Title"] = "DetalleMantenimeinto";
}
@model List<ObtenerViajesClienteViewModel>

@{
    var permisosPagina = await UsuarioHelper.GetPermisosPagina(User, EnumOpcionMenu.gestionDeClientes);
    if (permisosPagina == null)
    {
        return;
    }
}

<table class="table align-middle table-row-dashed fs-6 gy-5" id="kt_table_datos_vehiculo">
    <thead>
        <tr class="text-start text-muted fw-bold fs-7 text-uppercase gs-0">
            <th class="min-w-125px">Marca</th>
            <th class="min-w-125px">Modelo</th>
            <th class="min-w-125px">Mecanico</th>
            <th class="min-w-125px">Placas</th>
            <th class="min-w-125px">Número de Poliza</th>
            <th class="min-w-125px">Número de Motor</th>
        </tr>
    </thead>
    <tbody class="text-gray-600 fw-semibold">
        @*  @foreach (var item in Model)
        {
        <tr>
        <td class="date-cell">
        @{
        var fecha = item.Fechaviaje != null ? Convert.ToDateTime(item.Fechaviaje).ToString("dd/MM/yyyy  HH:mm") : string.Empty;
        }
        @fecha
        </td>
        <td>
        @item.Ruta
        </td>
        <td>
        <input type="hidden" data-kt-usuario-table-filter="row_id" value="@item.ViajeId" />
        @item.PuntoAbordaje
        </td>
        <td>
        @item.HoraAbordaje
        </td>
        <td>
        @item.PuntoDescenso
        </td>
        <td>
        @item.HoraDescenso
        </td>
        <td>
        @item.Corrida
        </td>
        <td>
        @item.Precio
        </td>
        <td>
        @item.Pasajeros
        </td>
        </tr>

        } *@
    </tbody>
</table>
