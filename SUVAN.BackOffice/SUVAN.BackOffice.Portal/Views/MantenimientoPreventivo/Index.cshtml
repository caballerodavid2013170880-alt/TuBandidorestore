@using SUVAN.BackOffice.Models.ViewModel.Enums;
@using SUVAN.BackOffice.Portal.Helper;
@inject IUsuarioHelper UsuarioHelper
@{
    ViewData["Title"] = "Mantenimiento Preventivo";
}
@model List<SUVAN.BackOffice.Database.Entities.Preventivo>
@{
    var permisosPagina = await UsuarioHelper.GetPermisosPagina(User, EnumOpcionMenu.MantenimientoPreventivo);
    if (permisosPagina == null) { return; }
}

<link href="~/css/logistica/preventivo/tabs-preventivo.css" rel="stylesheet" type="text/css" />

<div id="kt_app_toolbar" class="app-toolbar">
    <div id="kt_app_toolbar_container" class="app-container container-fluid d-flex align-items-stretch">
        <div class="app-toolbar-wrapper d-flex flex-stack flex-wrap gap-4 w-100">
            <div class="page-title d-flex flex-column justify-content-center gap-1 me-3">
                <h1 class="page-heading text-dark fw-bold fs-3 m-0">@ViewData["Title"]</h1>
            </div>
            <div class="d-flex align-items-center gap-2 gap-lg-3">
                @if (permisosPagina!.Agregar)
                {
                    <a href="~/MantenimientoPreventivo/AgregarPreventivo" class="btn btn-flex btn-primary h-40px fs-7 fw-bold">Agregar Preventivo</a>
                }
            </div>
        </div>
    </div>
</div>

<div id="kt_app_content" class="app-content flex-column-fluid">
    <div id="kt_app_content_container" class="app-container container-fluid">
        <div class="d-flex align-items-center mb-8 overflow-auto">
            <a href="~/MantenimientoPreventivo/Index" class="nav-tab-custom nav-tab-active">Mantenimientos</a>
            <a href="~/MantenimientoPreventivo/DetallePreventivos" class="nav-tab-custom">Detalle de Preventivos</a>
        </div>

        <div class="card">
            <div class="card-header border-0 pt-6 d-flex flex-column">
                <div class="row w-100 mb-4">
                    <div class="col-md-3">
                        <div class="d-flex align-items-center position-relative my-1">
                            <i class="ki-outline ki-magnifier fs-3 position-absolute ms-5"></i>
                            <input type="text" data-kt-preventivo-table-filter="search" class="form-control form-control-solid ps-13" placeholder="Buscar texto..." />
                        </div>
                    </div>
                    <div class="col-md-2"><input type="text" id="filtroNombre" class="form-control form-control-solid" placeholder="Nombre..." /></div>
                    <div class="col-md-2"><input type="text" id="filtroPlanta" class="form-control form-control-solid" placeholder="Planta..." /></div>
                    <div class="col-md-2"><input type="text" id="filtroMarca" class="form-control form-control-solid" placeholder="Marca/Modelo..." /></div>
                    <div class="col-md-3"><input type="date" id="filtroFecha" class="form-control form-control-solid" /></div>
                </div>
            </div>

            <div class="card-body py-4">
                <table class="table align-middle table-row-dashed fs-6 gy-5" id="kt_table_preventivo">
                    <thead>
                        <tr class="text-start text-muted fw-bold fs-7 text-uppercase gs-0">
                            <th class="min-w-250px">Nombre</th>
                            <th class="min-w-150px">Planta / Depósito</th>
                            <th class="min-w-150px">Marca / Modelo</th>
                            <th class="min-w-125px">Costo Total</th>
                            <th class="min-w-125px">Fecha Prevista</th>
                            <th class="text-end min-w-100px">Acciones</th>
                        </tr>
                    </thead>
                    <tbody class="text-gray-600 fw-semibold">
                        @foreach (var prev in Model)
                        {
                            <tr>
                                <td class="text-wrap mw-250px">@prev.NombrePreventivo</td>
                                <td>@(prev.IdPlantaNavigation?.NombrePlanta) / @(prev.IdDepositoNavigation?.NombreDeposito)</td>
                                <td>@(prev.IdMarcaNavigation?.Descripcion) / @(prev.IdModeloNavigation?.Descripcion)</td>
                                <td><span class="badge badge-light-success">@((prev.CostoTotal ?? 0).ToString("C"))</span></td>
                                <td>@prev.FechaPrev.ToString("dd/MM/yyyy")</td>
                                <td class="text-end">
                                    <button class="btn btn-icon btn-light btn-active-light-primary w-30px h-30px me-1" onclick="abrirModalDetalle(@prev.Idpreventivo)" title="Ver detalles del plan preventivo">
                                        <i class="ki-outline ki-eye fs-3"></i>
                                    </button>
                                    @if (permisosPagina!.Editar)
                                    {
                                        <a href="~/MantenimientoPreventivo/AgregarPreventivo/@prev.Idpreventivo" class="btn btn-icon btn-light btn-active-light-primary w-30px h-30px" title="Editar preventivo">
                                            <i class="ki-outline ki-pencil fs-3"></i>
                                        </a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" tabindex="-1" id="modalDetallePreventivo">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content" id="modalContentAjax">
        </div>
    </div>
</div>

@section scripts {
    <script src="~/js/logistica/preventivo/table.js" asp-append-version="true"></script>
}